# Minimal config exercising workflow-plugin-crm.
# Schema-validate with: wfctl validate --skip-unknown-types examples/minimal/config.yaml
version: 1
modules:
  - name: crm-provider
    type: crm.provider
    config: {}
workflows:
  pipeline:
    steps:
      - name: example
        type: step.crm_create_record
        config: {}
